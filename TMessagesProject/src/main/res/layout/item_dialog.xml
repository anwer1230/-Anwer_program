<?xml version="1.0" encoding="utf-8"?>
<!-- TMessagesProject/src/main/res/layout/item_dialog.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:background="?android:attr/selectableItemBackground"
    android:paddingStart="12dp"
    android:paddingEnd="12dp">

    <!-- User / Group Avatar -->
    <ImageView
        android:id="@+id/dialog_avatar"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_centerVertical="true"
        android:contentDescription="Avatar" />

    <!-- Title and Verification Badge -->
    <LinearLayout
        android:id="@+id/dialog_title_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="12dp"
        android:layout_marginTop="14dp"
        android:layout_toEndOf="@id/dialog_avatar"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/dialog_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="#ffffff"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Timestamp -->
    <TextView
        android:id="@+id/dialog_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="15dp"
        android:textColor="#708499"
        android:textSize="12sp" />

    <!-- Subtitle / Last Message Preview -->
    <TextView
        android:id="@+id/dialog_message_preview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/dialog_title_container"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:layout_toEndOf="@id/dialog_avatar"
        android:layout_toStartOf="@+id/dialog_unread_badge"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#8e9ba8"
        android:textSize="14sp" />

    <!-- Unread Badge or Pin Icon -->
    <TextView
        android:id="@+id/dialog_unread_badge"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/dialog_time"
        android:layout_marginTop="4dp"
        android:background="#2481cc"
        android:gravity="center"
        android:minWidth="20dp"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:textColor="#ffffff"
        android:textSize="12sp"
        android:textStyle="bold" />

</RelativeLayout>
